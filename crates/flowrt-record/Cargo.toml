[package]
name = "flowrt-record"
version = "0.7.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"

[dependencies]
mcap = { version = "0.24.0", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
serde_json = "1"
